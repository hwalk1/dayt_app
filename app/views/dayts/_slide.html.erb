<% trip_dayt =  TripDayt.new %>
  <%= simple_form_for [trip, trip_dayt],
      html: {
      data: { action: "submit->create-trip-dayt#create", create_trip_dayt_target: "form" }
      } do |f| %>
      <%= f.hidden_field :dayt_id, value: dayt.id  %>
  <%= f.submit %>
<% end %>
<%= dayt.title %>
<%= dayt.location %>
<%= dayt.duration %>
<%= dayt.price %>
